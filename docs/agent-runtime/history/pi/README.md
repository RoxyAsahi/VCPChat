# 已归档：Pi-era Agent Runtime 资料

本目录及 `adr/` 保存 2026-07-29 之前的 Pi Worker、多 Driver、SQLite、`vcp_delegate`、submodule 与旧测试矩阵资料。它们仅用于历史追溯，**不能**定义当前实现、当前计划或验收条件。

当前唯一真源为 [../../current/README.md](../../current/README.md) 中列出的四份文档。当前产品路径是：Electron thin supervisor → 仓库内 Rust `vcp-agentd --direct` → VCPToolBox。
