# 2026-03-29 上游对齐与后续调整记录

## 本次动作
- 本地开发基线已对齐到最新 `upstream/main`
- 对齐前保留了本地安全备份分支：`backup/feature-chat-ui-upgrades-pre-upstream-sync-20260329`
- 丢弃了未提交的 `styles/themes.css` 实验性本地改动，以上游版本为准
- 恢复了此前保留的本地开发文档，继续作为仓库内开发资料使用

## 上游合并后调整记录

### 1. `7540ba2` 引入一些像素级微调
- 放大左侧助手/话题列表的高度、头像和文字，使列表层级更突出
- 收轻顶部与底部按钮的默认态，弱化原本较重的玻璃感
- 隐藏部分标题元素，让主界面结构更干净
- 影响：
  - 左侧列表的视觉重心明显增强
  - 主界面开始从“装饰感较强”转向“结构感更清晰”

### 2. `a31f3e5` 优化多个 UI 布局
- 删除群组“统一模型”相关配置 UI
- 将模型选择器箭头改为更简洁的下拉图标，并缩小按钮尺寸
- 继续微调顶部 `CREATE / TOPIC` 按钮的 SVG 对齐
- 将主题整体切回 `Code IDE` 风格
- 影响：
  - 设置页功能块进一步瘦身
  - 控件系统和主题语言开始重新收口

### 3. `47d6292` fix
- 主要重排 `main.html` 内的大量设置页和弹窗 HTML 结构
- 重组助手设置、全局设置、渲染设置等区域的布局层次
- 明显增强 Rust 划词助手的状态信息、诊断信息和可观测性展示
- 影响：
  - 后续 CSS 精修更容易建立在稳定结构上
  - 高级功能区从“能配置”转向“可诊断、可观察”

### 4. `a375913` 微调一些细节
- 放大部分按钮文字尺寸
- 给左侧搜索胶囊增加更松一点的上下间距
- 影响：
  - 属于典型的像素级观感修正
  - 用于缓和前一轮结构调整后的紧凑感

### 5. `7a5c7c0` 微调 UI
- 继续对主界面做小幅视觉修正，作为上述几次调整的延续
- 与前几个提交共同构成“PR 合并后的上游二次打磨”
- 影响：
  - 说明主界面在合并后仍处于连续细修阶段
  - 后续开发时需要默认以上游当前视觉结果作为新基线

## 后续开发约定
- 继续开发时，以当前 `upstream/main` 视觉与结构结果为默认基线
- 本地 `docs` 可继续保留并迭代，但默认不混入后续面向上游的 PR
- 如果后续再做 UI 精修，建议优先记录：
  - 改动意图
  - 最终保留结果
  - 是否与上游后续提交存在覆盖或冲突
