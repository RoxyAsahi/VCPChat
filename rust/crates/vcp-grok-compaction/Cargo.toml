# Derived from grok-build/crates/common/xai-grok-compaction at the revision
# recorded in ../GROK_SOURCE_PROVENANCE.md. Apache-2.0 notices are retained.
[package]
name = "vcp-grok-compaction"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license = "Apache-2.0"
description = "VCP-vendored transport-agnostic compaction primitives from Grok Build"

[lib]
path = "lib.rs"

[dependencies]
anyhow.workspace = true
async-trait.workspace = true
serde.workspace = true
thiserror.workspace = true
tokio = { workspace = true, features = ["time", "macros"] }
tracing.workspace = true

[dev-dependencies]
serde_json.workspace = true
tokio = { workspace = true, features = ["rt-multi-thread"] }
