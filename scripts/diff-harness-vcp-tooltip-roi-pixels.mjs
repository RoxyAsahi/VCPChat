import crypto from 'node:crypto';import fs from 'node:fs';import path from 'node:path';
const root=process.cwd(),read=n=>fs.readFileSync(path.join(root,'reports',n));const harness=read('harness-tooltip-source.png'),vcp=read('vcp-tooltip-candidate.png');const report={generatedAt:new Date().toISOString(),comparison:'strict same-engine Tooltip ROI PNG byte comparison',harness:{file:'harness-tooltip-source.png',bytes:harness.length,sha256:crypto.createHash('sha256').update(harness).digest('hex')},vcp:{file:'vcp-tooltip-candidate.png',bytes:vcp.length,sha256:crypto.createHash('sha256').update(vcp).digest('hex')},exactPixelPass:harness.equals(vcp),pass:harness.equals(vcp),note:'Strict byte equality is a pixel pass for the fixed ROI only; it does not close the independently observed parent ownership mismatch or any production-consumer boundary.'};fs.writeFileSync(path.join(root,'reports/harness-vcp-tooltip-roi-pixel-diff.json'),JSON.stringify(report,null,2)+'\n');console.log(`Harness↔VCP Tooltip ROI pixels: exact=${report.exactPixelPass}; harnessBytes=${harness.length}; vcpBytes=${vcp.length}.`);
