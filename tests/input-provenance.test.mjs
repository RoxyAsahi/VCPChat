import assert from 'node:assert/strict'; import {execFileSync} from 'node:child_process'; import fs from 'node:fs'; import path from 'node:path'; import test from 'node:test';
const root=path.resolve(path.dirname(new URL(import.meta.url).pathname),'..'); test('Input provenance remains source-only',()=>{execFileSync(process.execPath,['scripts/check-harness-input-source-provenance.mjs'],{cwd:root,stdio:'pipe'});const r=JSON.parse(fs.readFileSync(path.join(root,'reports/harness-input-source-provenance.json'),'utf8'));assert.equal(r.files.every(f=>f.pass),true);assert.equal(r.reference.dom,true);assert.equal(r.reference.geometry,true);assert.equal(r.candidate.present,false);assert.equal(r.pass,false);});
